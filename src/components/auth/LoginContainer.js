import React from 'react'

export const LoginContainer = ({ children }) => {
    return (
        <div className="flex min-h-screen max-h-screen bg-bgimage justify-center items-center bg-cover bg-no-repeat">{children}</div>
    )
}
